import { Helmet } from "react-helmet";


const MyProfile = () => {
    return (
        <div>
             <Helmet>
                <title>Profile || Dune Shop</title>
            </Helmet>
            this is my profile
        </div>
    );
};

export default MyProfile;