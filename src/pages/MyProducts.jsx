import { Helmet } from "react-helmet";

const MyProducts = () => {
    return (
        <div>
             <Helmet>
                <title>My Products || Dune Shop</title>
            </Helmet>
            this is my products
        </div>
    );
};

export default MyProducts;