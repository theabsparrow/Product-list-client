import { Helmet } from "react-helmet";


const AddProduct = () => {
    return (
        <div>
             <Helmet>
                <title>Add Products || Dune Shop</title>
            </Helmet>
            add your product
        </div>
    );
};

export default AddProduct;